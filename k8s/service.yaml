apiVersion: v1
kind: Service

metadata:
  name: hr-portal-service

spec:
  type: NodePort

  selector:
    app: hr-portal

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080